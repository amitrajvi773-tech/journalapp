# journalapp
A Spring Boot REST API project for managing journal entries using MySQL database and Spring Data JPA. The application performs CRUD operations such as creating, reading, updating, and deleting journal entries through REST APIs tested with Postman
